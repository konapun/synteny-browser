This is a sequence region comparison visualization which uses the Scribl HTML5 canvas library (http://chmille4.github.com/Scribl/).

Files
------

Example
-------
(HTML)
<html>
	<head>
		<title>Region Comparison</title>
	</head>
	<body>
		<div id="browser"></div>
	</body>
</html>

(Javascript)
var browser = new SyntenyBrowser(document.getElementById('browser'));
var seq1 = browser.addSequence(nucleotides1, seq1start);
var seq2 = browser.addSequence(nucleotides2, seq2start);

browser.draw();

var region1 = seq1.selectRegion(200, 200);
var region2 = seq2.selectRegtion(1, 100);

region1.compareWith(region2); // does alignment and displays result